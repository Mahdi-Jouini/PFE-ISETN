import { MemberRolePipePipe } from './member-role-pipe.pipe';

describe('MemberRolePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberRolePipePipe();
    expect(pipe).toBeTruthy();
  });
});
